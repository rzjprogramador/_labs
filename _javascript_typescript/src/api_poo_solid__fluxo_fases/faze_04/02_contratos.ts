/* eslint-disable no-unused-vars */

const contratos = {
    interface_repositorio: `Contratos metodos para serem implementados em cada ObjetivoDeCaso(CasoDeUso)
    exemplo: findByName(), create(), list(), update(), delete()`,

    DTO: `
    - São contratos das info que vem de fora ::
    - Tem as mesmas informacoes Obrigatorias da Entidade ...
    - Quem usa: para obedecer recebimento via params são os metodosDeConfiguracao das classes de       configuracao que expoem comandos
    - || para tipos que dependem de subtipos ex: type endereco que tem os sub niveis rua, numero, bairro etc...`
}
