{
    "compilerOptions": {
        /* Visit https://aka.ms/tsconfig.json to read more about this file */

        "target": "ES2019",                                   /* Specify ECMAScript target version: 'ES3' (default), 'ES5', 'ES2015', 'ES2016', 'ES2017', 'ES2018', 'ES2019', 'ES2020', or 'ESNEXT'. */
        "module": "commonjs",                             /* Specify module code generation: 'none', 'commonjs', 'amd', 'system', 'umd', 'es2015', 'es2020', or 'ESNext'. */
        "lib": ["ESNext", "DOM", "es5", "es6" ,"dom","es2015.collection"],
        "outDir": "./dist",
        "rootDir": "./src",
        "removeComments": true,
        "noEmitOnError": true,
        "strict": false,                                 /* Ative todas as opções de verificação de tipo restrita. */
        "allowSyntheticDefaultImports": true,           /* Evita problemas Import Export Modulos */
        "esModuleInterop": true,                       /* Habilita a interoperabilidade de emissão entre os Módulos CommonJS e ES por meio da criação de objetos de namespace para todas as importações. Implica 'allowSyntheticDefaultImports'. */
        "skipLibCheck": true,                         /* Pular a verificação de tipo de arquivos de declaração. */
        "forceConsistentCasingInFileNames": true,    /* Proibir referências com maiúsculas e minúsculas inconsistentes para o mesmo arquivo. */
        "strictPropertyInitialization": false,      /* Retirar erro de inicializacao variavel private no construtor */
        "allowJs": true, /* Permitir que arquivos javascript sejam compilados. */

        "baseUrl": "." /*Path Mappping -- Caminhos Mapeados -- caminhos com @ no import*/,
        "paths": {
            "@/*": ["./src/*"]
        }


    },

    "typeRoots": [                                   /* Tipos raizes no node Modules*/
        "node_modules/@types"
    ],

    "include": ["src/**/*.ts"],                     /* include para ele compilar o que esta dentro*/
        "exclude": [                               /* exclude para nao ficar recompilando ao compilar*/
            "node_modules",
            "dist",
            ".docker",
        ],
  }
