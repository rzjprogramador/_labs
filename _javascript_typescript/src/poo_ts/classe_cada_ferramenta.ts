/* eslint-disable no-unused-vars */

const topo_classe = {
    globalVariaveisInternaAclopada: 'fica no topo da classe --> declara props. interna da classe',
    construtor: {
        parametro: {
            contexto: `
            ONDE CONSTROI ::
            define propriedades da classe tanto que vem de dentro quanto de fora.
            `,

            props_que_vem_de_dentro: `- se o valor vem de dentro das variaveis da classe configura no bloco construtor`,

            props_que_vem_de_fora_integrada: `- se o valor vier de fora (integracao) : configura na instanciacao quando invocar a classe`,

            bloco_construtor: 'opção configurar ou pré configurar props. internaDaClasse ou vinda de fora pelo parametro'
        }

    }
}
